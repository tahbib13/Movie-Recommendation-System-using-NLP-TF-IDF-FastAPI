🎬 MovieMood – Movie Recommendation System using NLP

MovieMood is a content-based movie recommendation system built using Natural Language Processing (NLP).
It recommends similar movies based on movie descriptions, genres, and taglines.

The system processes textual movie metadata, converts it into numerical vectors using TF-IDF, and then calculates similarity using cosine similarity.
